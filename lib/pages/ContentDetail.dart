import 'package:flutter/material.dart';

class ContentDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Text("详细", style: TextStyle(fontSize: 20.0));
  }
}
